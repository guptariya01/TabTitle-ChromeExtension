# TabTitle-ChromeExtension
I made a Chrome Extension which has a button in the popup , when button is clicked the extension automatically pick up the title of the current tab and show it in popup itself.
